
public class MissileDefense {
	public static void main(String[] args) {
		// double distance;
		// secretx = something;
		// secrety = something;
		
		// DO {
			// ask the user to guess an x coordinate
			// ask the user to guess the y coordinate
		
			// calculate the distance
		
			// if (distance is 0) {
			//     display "you got it"
			// }
		
			// if (distnace > 0) {
			//		display "you missed"
			// }
		// } WHILE (distance > 0);
	}
}
